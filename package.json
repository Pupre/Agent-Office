{
  "name": "agent-office",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @ai-workflow/web",
    "dev:server": "npm run dev --workspace @ai-workflow/server",
    "build:web": "npm run build --workspace @ai-workflow/web",
    "start:server": "npm run start --workspace @ai-workflow/server"
  }
}
